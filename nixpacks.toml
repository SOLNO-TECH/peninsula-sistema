# Dokploy Nixpacks — force fresh build
# bust: 2026-09-18-v2

[phases.setup]
nixPkgs = ["nodejs_20"]

[phases.install]
cmds = ["npm ci"]

[phases.build]
cmds = [
  "export VITE_APP_BUILD=2026-09-18-v2",
  "npm run build",
  "echo 2026-09-18-v2 > dist/build-id.txt",
  "echo 2026-09-18-v2 > BUILD_ID",
  "test -f dist/index.html"
]

[start]
cmd = "node server/index.js"
