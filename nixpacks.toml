# Dokploy · Nixpacks
# Usa Node 20 LTS + Vite 6 (sin Rolldown nativo)

[phases.setup]
nixPkgs = ["nodejs_20"]

[phases.install]
cmds = ["npm ci"]

[phases.build]
cmds = ["npm run build"]

[start]
cmd = "npx --yes serve -s dist -l ${PORT:-3000}"
