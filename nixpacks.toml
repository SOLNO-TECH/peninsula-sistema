# Dokploy Nixpacks
# Node 20 + Express API + static frontend
# cache-bust: hero-v2-2026-07-23

[phases.setup]
nixPkgs = ["nodejs_20"]

[phases.install]
cmds = ["npm ci"]

[phases.build]
cmds = ["npm run build"]

[start]
cmd = "node server/index.js"
